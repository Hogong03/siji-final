# 思迹 AI 技术升级方案（2026-08-27）

> 调研结论 + 多方案比选。信息源：各厂商官方文档（2026-08-27 抓取）。
> 标注 [官方确认] = 已从官方文档/官网核实；[待验证] = 需实测或到控制台确认。
> 结论先行：模型层必须升级（通义/Kimi 已过时），能力层优先做「拍照记账 OCR」和「语音输入」。

---

## 一、模型层升级（必做，改动小收益最大）

现状：providers.js 注册表式配置，换模型只需改配置 + 适配参数。各厂商 2026-08 最新状态：

| 厂商 | 项目当前 | 2026-08 最新 [官方确认] | 升级要点 |
|---|---|---|---|
| DeepSeek | V4 Flash / V4 Pro | V4 Flash(0731) / V4 Pro(0813) / V4-Flash-Vision(实验) | 基本已最新，补 Vision 实验模型选项 |
| 智谱 GLM | 4-flash / 4.7 / 5.1 / 5.2 | **GLM-5.3 / GLM-5.3-Flash**（1M 上下文、128K 输出） | 见 A1 迁移注意 |
| 通义千问 | turbo / plus / max / long | **qwen3.7-flash / qwen3.7-plus / qwen3.8-max** | 模型 ID 全换 |
| Moonshot | k2.5 / k2.6 / k2.7-code | **Kimi K3**（2.8T 参数、1M 上下文、多模态、Tool Calling） | 模型 ID 换 k3 |

### A1 智谱 GLM-5.3（推荐主力）
- [官方确认] 1M 上下文 / 128K 输出；编程较 5.2 提升 50%；Agent/长程任务 SOTA 级。
- **迁移硬约束**：GLM-5.3 强制开启思考，参数 `thinking.type: enabled` + `reasoning_effort: low|high|max`。若项目现在传 `thinking.type: disabled` 会直接报错，必须在 providers.js 按模型适配。
- 支持 Function Calling / 上下文缓存 / 结构化输出 / 流式工具输出，与项目 Agent Loop 完全匹配。
- 端点：`https://open.bigmodel.cn/api/paas/v4`（OpenAI 兼容）+ 新增 `/api/v1`（OpenAI Response 协议）。

### A2 通义 qwen3.7 / qwen3.8（推荐升级）
- [官方确认] qwen3.7-flash（快）/ qwen3.7-plus / qwen3.8-max（旗舰）。
- 现有 qwen-turbo/plus/max/long 已不是当前线，建议直接替换。

### A3 Kimi K3（推荐升级）
- [官方确认] 2026-07-16 上线旗舰：2.8T 参数、1M 上下文、多模态、Tool Calling。
- 适合做「多模态 + 长对话」主力之一；k2.x 保留作降级。

### A4 DeepSeek V4（保持 + 补视觉）
- [官方确认] v4-flash/v4-pro 已是当前线；新增 `deepseek-v4-flash-vision-exp` 进 visionModels。

**推荐默认组合**：主力 GLM-5.3（Agent/长任务）+ 快对话 DeepSeek V4 Flash + 视觉 Kimi K3（或 qwen3.5-omni-plus）。四厂商可切换不动摇。

---

## 二、拍照记账 / OCR（推荐，杀手级功能）

### B1 智谱 GLM-OCR [官方确认]
- 专用布局解析 API：`POST https://open.bigmodel.cn/api/paas/v4/layout_parsing`，model=glm-ocr，传图片 URL/文件。
- 支持文档/表格/图片识别并输出结构化内容 → 小票/发票拍照自动记账。
- 与 GLM 同厂商密钥复用；项目新增一个 vision/ocr 请求分支即可。

### B2 模型内视觉（多模态）
- Kimi K3、qwen3.5-omni-plus、GLM-5.3-Flash、DeepSeek v4-flash-vision-exp 均支持图像理解。
- 适合「日记配图理解/氛围总结」，不需要专门 OCR 服务；项目已有 vision-bridge.js，扩展模型列表即可。

### B3 端侧 OCR（离线可选）
- PaddleOCR 小程序插件/本地库：离线、隐私最好，但准确率低于云端、维护成本高。

---

## 三、语音交互（推荐，语音记账/口述日记）

### C1 通义实时语音（推荐首选）
- [官方确认] `qwen-audio-3.0-realtime-plus`（实时语音对话）、`qwen-audio-3.0-asr-flash`（文件/流式识别）、`qwen-audio-3.0-tts-plus`（合成）。
- 同属阿里云 Model Studio，与 qwen 文本模型密钥统一；三端 WebSocket 支持需实测。

### C2 智谱语音
- [官方确认] GLM-ASR-2512 + GLM-TTS，与 GLM 文本模型同厂商，密钥统一。

### C3 豆包实时语音（待验证）
- 火山方舟有实时语音方案，具体模型 ID 需到控制台确认。

### C4 OpenAI Realtime API（国际，可选）
- 移动端 WebSocket 实时语音成熟；国内需代理，不适合默认三端方案。

---

## 四、Agent 引擎升级（工具层）

### D1 MCP 标准化（远期）
- [官方确认] MCP 规范 2026-07-28 版已支持远程 MCP 服务器 + 官方客户端最佳实践 + SDK。
- 把现有 31 工具按 MCP 规范组织，未来可对接第三方 MCP 服务器（日历/支付/健康等）。
- uni-app 前端做 MCP 客户端需引入 SDK；tools.js 已是 facade，先做工具层抽象成本可控。

### D2 联网搜索工具（推荐）
- [官方确认] 智谱提供联网搜索能力（web-search）；GLM-5.3 配搜索后可回答实时类问题。
- 通义/DeepSeek 搜索 API 需确认；也可接通用 Search API。
- 与现有 QUERY_TOOLS 只读自动执行机制天然契合。

### D3 上下文缓存 + 结构化输出（推荐，降本提速）
- [官方确认] GLM 上下文缓存（长对话降本）；GLM 结构化输出（JSON schema 强约束，替代"response_format 不稳定就靠 prompt"的补丁）。
- 通义 json mode / 缓存待验证；DeepSeek 项目已标 supportsResponseFormat=true。
- 落地：providers.js 增加 `supportsStructuredOutput`、`supportsContextCache` 字段，按厂商/模型路由。

### D4 推理分级
- GLM-5.3 reasoning_effort（low/high/max）按任务动态切换：闲聊/快速查询用 low，复杂决策用 max。
- 结合现有「动态确认阈值」机制，按工具类型决定推理强度。

---

## 五、记忆与个性化

### E1 结构化记忆升级（推荐，纯本地可落地）
- 把 `siji_long_term_memory` 从"文本堆"升级为「实体（人/事/物）+ 事件 + 关系」结构化记忆，AI 写入时自动归档，读取时按相关度注入 prompt。
- 三端可跑、无服务端依赖；配合现有 prompt-builder 动态段注入。

### E2 向量语义检索（服务端可选）
- [官方确认] 智谱 Embedding-3 / qwen3-rerank（重排）可用于语义检索。
- 服务端轻量网关 + 向量库（sqlite-vec / pgvector / Milvus）；纯客户端受小程序存储限制，本地 JSON 索引可先顶。
- 对应项目已有 `siji_index` 搜索索引，可平滑升级。

### E3 对话历史压缩
- 用长上下文模型（GLM-5.3/K3 1M）对历史定期摘要归档，控制 prompt token 成本。

---

## 六、工程与合规（顺带）

- F1 API Key 加密升级：XOR+Base64 → AES-GCM（或服务端网关代理，Key 不进客户端）。隐私红线，推荐做。
- F2 模型注册表已是配置式，升级改动集中在 providers.js + 请求参数适配 + 厂商能力位（thinking / reasoning_effort / structured output / cache）。
- F3 每项升级按 AGENTS.md 要求：manifest versionName 提升 + version-data.js 版本记录。

---

## 推荐执行顺序（供挑选）

| 优先级 | 项 | 改动量 | 收益 |
|---|---|---|---|
| P0 | A1/A2/A3 模型升级 + A4 补视觉 | 小 | 立即变强 |
| P0 | B1 拍照记账 GLM-OCR | 中 | 杀手级功能 |
| P1 | C1 语音输入（通义实时语音） | 中 | 体验质变 |
| P1 | D3 缓存+结构化输出、D2 联网搜索 | 小-中 | 降本+实时性 |
| P2 | E1 结构化记忆 | 中 | 个性化 |
| P2 | D1 MCP 标准化 | 大 | 生态扩展 |
| P3 | E2 向量检索（服务端） | 大 | 全局语义搜索 |
| P3 | F1 Key 加密升级 | 小 | 安全合规 |

---
信息来源（2026-08-27 抓取）：
- DeepSeek: https://api-docs.deepseek.com/
- 智谱: https://docs.bigmodel.cn/llms.txt（GLM-5.3 / GLM-OCR / 语音 / Embedding）
- 通义: https://www.alibabacloud.com/help/en/model-studio/models
- Kimi: https://kimi.com / https://platform.kimi.com（K3 发布信息）
- MCP: https://modelcontextprotocol.io/llms.txt（2026-07-28 版）