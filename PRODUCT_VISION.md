# 思迹产品愿景（PRODUCT VISION）

> 隐私声明：本文件包含用户个人健康状况描述，属敏感信息，禁止随仓库外发、截图外传或进入任何公开渠道。推广/开源前必须整体脱敏改写。

---

## 0. 服务对象

思迹只为一个人设计：朱红庚。

- 轻度抑郁症，INFP，生活中缺乏动力，爱好幻想，眼高手低，思想远大于行动
- 疲于人际交往，能量容易被社交耗尽
- 对金钱无感但实际缺钱

应用的唯一目标：**帮助他回归正常生活轨迹**。先解决"他"的问题，再谈产品化。

## 1. 一句话定位

思迹是低动力状态下的生活恢复系统：把「想」低成本地变成「做」。

服务的第一用户是"此刻连手指都不想动的朱红庚"。所有功能都过一道题：
**这个操作需要付出多少意志力？大于零就要重新设计。**

## 2. 核心闭环：说 → 沉淀 → 最小行动

```
你说话（唯一高成本动作由 AI 承担）
  ↓
AI 沉淀（记录/画像/记忆/关系，可确认可纠错）
  ↓
AI 给最小行动（5 分钟内可完成的第一步）
  ↓
你只做两件事：说话、点确认
```

闭环成立的判据：每次对话结束时，要么沉淀了新资产，要么更新了旧资产，要么产出了下一步最小行动，要么被明确拒绝。

## 3. 困境 × 产品对策

| 困境 | 对策 | 落点 |
|------|------|------|
| 缺乏动力 | 对话即记录，零表单；正反馈即时化 | chat 为主入口，diary/bill 手动录入全部降级为辅助 |
| 幻想多、眼高手低 | 想法落地管道：AI 拆出「5 分钟内能做的一步」，只确认第一步 | 计划模块默认小计划；识别并标记「计划性逃避」 |
| 想大于做 | 反复规划不执行时温和点破并缩小计划，不生成宏伟计划；随时可「先放一放」冷藏，恢复原样不丢进度 | AI 整理环节增加「执行核查」+ 计划冷藏（3.4 M3） |
| 疲于人际 | 社交能量预算：配额自定、AI 起草回复、延后不消失 | relation + 对话模板 |
| 金钱无感但缺钱 | 不靠记账习惯：对话/图片自动归账，每周播报三个数字 | bill 自动记账 + 周播报 |

## 4. 数据资产（整理层的产物）

| 资产 | 来源 | 现状载体 |
|------|------|----------|
| 画像（我的信息） | 对话 + 手动编辑 | `siji_my_profile` |
| 长期记忆 / 月结 | 对话自动 + 定期归纳 | `siji_long_term_memory` / `siji_monthly_memory` |
| 记录（日记） | 对话 + 主动记录 | `diary_YYYY-MM` |
| 计划 | 对话 + 主动制定 | `plan_all` |
| 账单 | 对话 + 自动归账 | `bill_YYYY-MM` |
| 人脉关系 | 对话 + 主动维护 | `siji_relations` / `siji_interactions` |
| 决策复盘 | 对话触发 | `siji_decisions` |
| 状态时间线 | 对话情绪识别 | 记忆/画像扩展字段 |

## 5. AI 的三个角色

| 角色 | 职责 | 质量判据 |
|------|------|----------|
| 记录员 | 听懂话语并落库，成本为零 | 说「记一下」后给出可编辑的落库预览 |
| 整理师 | 归纳特征、拆解行动、识别状态 | 连续低落时提醒休息，不催进度；静止是合法状态，未完成信息只有用户主动问才出现；逃避点破仅限用户点名该计划且可给出「冷藏」选项 |
| 顾问 | 状态觉察 → 最小行动 → 社交预案 → 财务真相 | 建议能引用「你上个月说过……」 |

## 6. 系统铁律（反模式清单）

1. **不制造新的失败感**：不打卡、不连击、不评分、断更不惩罚。任何「你应该更努力」的机制都是 bug。
2. **可改 > 记全**：记录错了可改，比记全更重要。
3. **AI 写入可确认**：自动落库前可预览，落库后可撤销。
4. **计划必须小**：大计划必须拆到第一步才能存在；反复规划不执行时缩小而非加大。
5. **识别规划与行动**：提醒用户区分「又想了 10 分钟」和「做了 5 分钟」。
6. **用户只做两件事**：说话、点确认。其余交互全部由 AI 主动完成。
7. **静止是合法状态**：一整天什么都没做，AI 不追问、不暗示、不惋惜。休息本身就是恢复的一部分。
8. **压力来自期待，不来自记录**：未完成信息（缺口/欠账/断更）只有用户主动问才出现，绝不主动盘点「还差什么」。
9. **弹性默认**：任一计划可一键「冷藏」（先放一放）；冷藏期 AI 不提、不算、不催，恢复后原样回来。

## 7. 与现状的差距（按影响排序）

1. 整理层缺失：AI 回复完即结束，无自动沉淀（现象：自定义属性不新增、计划变更新增而非修改）。
2. 对话是入口，但落库不可预览确认：AI 写入缺「预览→确认→撤销」闭环。
3. 建议不引用资产：回复停留在泛聊天，AI 显得没记性。
4. 计划模块鼓励「大而全」：与铁律 4 冲突，需重构为小步确认制。
5. 账单靠手动习惯：与用户「对金钱无感」冲突，需自动归账 + 周播报。

## 8. 迭代优先级（自用期）

| 优先级 | 内容 | 验收口径 |
|--------|------|----------|
| P0 | 对话 → 自动沉淀 → 预览确认 → 撤销 | 说「新增属性/变更计划」，改的是原资产而非新建 |
| P1 | 最小行动管道：AI 每次把想法收敛成 5 分钟第一步 | 一次对话后，屏幕上有一个可点的下一步 |
| P2 | 状态觉察：对话情绪识别 + 低点提醒（不催） | 连续两天低能量时有提示，无任何「加油」文案 |
| P3 | 金钱真相：自动归账 + 每周三数字播报 | 不看账单也知道本周花了多少 |
| P4 | 社交能量预算 + AI 起草回复 | 应付式社交有现成模板，可延后不消失 |

推广（多用户、云同步、隐私合规）是自用验证之后的事；推广前本文件必须脱敏改写。

## 9. 边界

- 思迹不做医疗诊断与治疗建议，不替代就医与用药。
- 医疗价值：情绪记录形成时间线供就医参考；状态监测提示「这周和上周不一样了」。

---
维护说明：本文件是产品决策依据，与 CODEX_HANDOFF.md、AGENTS.md 冲突时以本文件定位为准。更新任何产品功能前先读本文件。
