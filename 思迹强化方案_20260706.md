# 思迹 APP 强化方案

> 版本：v1.4.0 → v2.0 目标
> 编制日期：2026-07-06
> 编制依据：全量代码审查 + 项目历史记忆

---

## 一、现状诊断

### 1.1 已完成能力

| 模块 | 完成度 | 说明 |
|------|--------|------|
| AI 对话 | 85% | 流式输出、多轮记忆、摘要压缩、多模型切换、Agent 人设 |
| 日记 | 80% | CRUD、标签、AI 润色、心情、按月筛选 |
| 账单 | 85% | CRUD、预算、分类筛选、备注搜索、日期分组、左滑编辑、统计 |
| 计划 | 85% | CRUD、子任务、优先级、父子嵌套、提醒、模板、统计 |
| 个人信息 | 80% | AI 驱动 smart_update_profile、前端兜底提取、卡片式 UI |
| 长期记忆 | 70% | 自动提取、AI 摘要、CRUD、过期清理 |
| 语音输入 | 60% | 讯飞 WebSocket、H5/App 双通道、crypto-js 兼容问题未解决 |
| 管理后台 | 70% | Node+Express+MySQL+Vue3，设备/数据/AI/反馈管理 |
| 设置 | 80% | AI 配置、Agent 管理、隐私 PIN、数据导出、记忆、反馈 |

### 1.2 核心短板

| 编号 | 问题 | 影响 |
|------|------|------|
| P0-1 | **chat/index.vue 64000 字节**（单文件巨型） | 维护困难，热更新慢 |
| P0-2 | **store/data.js 38000 字节**（单文件巨型） | 状态管理混乱，难以扩展 |
| P0-3 | **AI 走神问题**：非 OpenAI 模型不强制 JSON，系统提示词虽已压缩但仍可能被历史消息淹没 | 用户感知 AI "不在线" |
| P0-4 | **pages.json TabBar 文本乱码**（`鎬濊抗` 应为 `思迹`） | APP 底部 Tab 显示乱码 |
| P1-1 | **语音输入未完全可用**：crypto-js UMD 导入问题、App 端未真机验证 | 核心交互缺失 |
| P1-2 | **无数据可视化**：账单/计划统计页纯文字+进度条，无图表 | 体验偏简陋 |
| P1-3 | **无搜索全局入口**：日记/账单/计划各自独立搜索，无统一搜索 | 用户找数据效率低 |
| P1-4 | **对话管理 UX 粗糙**：侧滑面板操作不直觉，新建对话命名规则简单 | 多会话体验差 |
| P1-5 | **无通知推送**：本地提醒仅 App.vue 60s 轮询，无系统通知栏 | 提醒形同虚设 |
| P2-1 | **crypto-js.min.js 60KB** 直接内联，增加包体积 | 包体积膨胀 |
| P2-2 | **theme.vue 空壳**（151 bytes）残留 | 废文件 |
| P2-3 | **feedback.vue 与 feedback-new.vue 并存** | 路由混乱 |
| P2-4 | **无单元测试 / E2E 测试** | 回归风险高 |

---

## 二、强化方案（分三个阶段）

### 阶段一：核心体验加固（P0，建议 3-5 天）

#### 1.1 修复 TabBar 乱码

**问题**：`pages.json` 中 TabBar 文本 `鎬濊抗`/`鍔熻兘`/`璁剧疆` 是 UTF-8 被当作 GBK 解码的乱码。

**方案**：
- 重写 `pages.json`，确保文件编码为 UTF-8 无 BOM
- TabBar 文本恢复为 `思迹`/`功能`/`设置`
- 同时检查 `manifest.json` 的 `name` 字段

#### 1.2 AI 响应可靠性强化

**问题**：非 OpenAI 模型（DeepSeek/智谱/通义/Kimi）不强制 JSON，AI 偶发走神或空回复。

**方案**（三层兜底）：

```
Layer 1 — 请求层：所有模型统一加 response_format
  ├─ OpenAI: response_format: { type: "json_object" }（已有）
  ├─ DeepSeek: response_format: { type: "json_object" }（新增，DeepSeek v3 支持）
  └─ 其他: 在 messages 末尾追加 { role: "system", content: "请只返回JSON" }

Layer 2 — 解析层：增强 parseAiResponse
  ├─ 提取 ```json ... ``` 代码块
  ├─ 容忍前后多余文字（找第一个 { 到最后一个 }）
  ├─ 解析失败时，尝试从纯文本提取 action 意图（已有的 fallback）
  └─ 仍然失败：返回 { reply: 原文, action: { type: "none" } }

Layer 3 — 空回复重试
  ├─ 空回复 → 自动重试 1 次（已有）
  ├─ 重试仍空 → 将用户消息精简后重试（去掉标记，保留核心意图）
  └─ 二次仍空 → 返回 "我刚才走神了，能再说一遍吗？"
```

#### 1.3 chat/index.vue 拆分

**问题**：单文件 64KB，包含对话管理、消息渲染、输入处理、AI 调用、执行结果、侧滑面板等。

**方案**：按职责拆分为 5-6 个组件 + 1 个页面壳：

```
pages/chat/index.vue          → 页面壳（300 行以内）
  ├─ components/chat/ConversationPanel.vue  → 侧滑会话管理面板
  ├─ components/chat/MessageList.vue        → 消息列表（scroll-view + 自动滚动）
  ├─ components/chat/MessageBubble.vue      → 已有，保持
  ├─ components/chat/ExecResultCard.vue     → 已有，保持
  ├─ components/chat/InputArea.vue          → 已有，保持
  └─ composables/useChatEngine.js           → AI 调用 + 执行 + 摘要触发逻辑
```

#### 1.4 store/data.js 拆分

**问题**：单文件 38KB，包含日记/账单/计划/标签/模板/统计所有 CRUD。

**方案**：

```
store/data.js          → 聚合入口（保持向后兼容）
  ├─ store/modules/diary.js    → 日记 CRUD + 查询
  ├─ store/modules/bill.js     → 账单 CRUD + 查询 + 预算
  ├─ store/modules/plan.js     → 计划 CRUD + 子任务 + 模板
  ├─ store/modules/stats.js    → 统计 + 看板数据
  └─ store/modules/tags.js     → 标签管理
```

---

### 阶段二：体验跃升（P1，建议 5-7 天）

#### 2.1 数据可视化

**当前**：统计页用文字 + 进度条展示数据。

**方案**：引入轻量图表库（uCharts 或自绘 Canvas），覆盖：

| 场景 | 图表类型 | 数据来源 |
|------|----------|----------|
| 账单月度概览 | 环形图（分类占比） | `query_stat` 返回数据 |
| 账单趋势 | 柱状图（近 7 天/30 天） | `weekTrend` |
| 计划完成率 | 仪表盘 | `planTotal / planCompleted` |
| 计划优先级分布 | 横向条形图 | `priorityMap` |
| 日记心情分布 | 点阵图/雷达图 | `mood` 统计 |

**技术选型**：
- 优先 `uCharts`（uni-app 生态原生支持，跨端兼容）
- fallback：自绘 Canvas（无第三方依赖）

#### 2.2 全局搜索

**方案**：在功能页顶部新增全局搜索入口，或通过 AI 对话直接搜索。

```
搜索入口设计：
  ├─ 功能页顶部搜索框 → 输入关键词
  ├─ 同时搜索日记(标题+内容) / 账单(备注+分类) / 计划(标题+描述)
  ├─ 结果按类型分组，点击跳转对应详情
  └─ 支持时间范围筛选（最近7天/30天/全部）
```

**实现**：
- `utils/storage.js` 新增 `globalSearch(keyword, options)` 函数
- 利用已有的 `rebuildIndex()` 索引机制
- 搜索结果页 `pages/search/result.vue`

#### 2.3 对话管理 UX 重构

**当前**：侧滑面板操作不直觉。

**方案**：

```
改进点：
  1. 对话列表从侧滑改为底部弹出 Sheet（更符合移动端习惯）
  2. 新建对话：自动命名为 "对话 N"（N=当前对话数+1），首条消息后 AI 自动生成标题
  3. 对话项操作：左滑显示「重命名」「删除」
  4. 对话标题自动生成：首轮对话后，调用 AI 生成 4-6 字标题
  5. 当前对话顶部显示标题（可点击修改）
```

#### 2.4 系统通知

**当前**：`App.vue` 60s 轮询，无系统通知。

**方案**：

```
本地通知调度（utils/reminder.js 增强）：
  ├─ uni.createPushMessage（APP 端创建本地通知）
  │   ├─ 计划到期前 30 分钟推送
  │   ├─ 通知标题：计划标题
  │   └─ 通知内容：截止时间 + 剩余子任务
  ├─ 小程序端：使用 subscribeMessage 订阅消息
  └─ H5 端：Notification API（需用户授权）
```

**关键代码路径**：
```javascript
// utils/reminder.js
function scheduleNotification(plan) {
  // #ifdef APP-PLUS
  plus.push.createMessage(plan.title, JSON.stringify({ planId: plan.id }), {
    title: '思迹提醒',
    sound: 'system'
  })
  // #endif
  // #ifdef H5
  if (Notification.permission === 'granted') {
    new Notification('思迹提醒', { body: plan.title })
  }
  // #endif
}
```

#### 2.5 语音输入修复

**问题**：crypto-js UMD 导入不兼容 ES Module。

**方案**（二选一）：

```
方案 A（推荐）：移除 crypto-js 依赖，用原生 Web Crypto API
  ├─ H5: crypto.subtle.sign('HMAC-SHA1', key, data) → base64
  ├─ App: uni 虽不支持 subtle API，但可用 thirdapp 的加密模块
  └─ 小程序: 同 App

方案 B：将 crypto-js 改为 ESM 格式导入
  ├─ npm install crypto-js（而非内联 min.js）
  └─ import HmacSHA1 from 'crypto-js/hmac-sha1'
```

推荐方案 A，彻底移除 60KB 的 crypto-js 依赖。

---

### 阶段三：打磨与扩展（P2，建议 3-5 天）

#### 3.1 清理与瘦身

| 项目 | 操作 |
|------|------|
| `theme.vue` | 删除空壳文件，移除路由 |
| `feedback.vue` | 与 `feedback-new.vue` 合并，保留一个 |
| `utils/theme.js` / `store/theme.js` | 已 stub 化，确认无引用后删除 |
| `utils/themeMixin.js` | 同上 |
| `crypto-js.min.js` | 改用 Web Crypto API 后删除（减 60KB） |
| 临时测试文件 | 清理 `markdown/` 下的开发日志 |

#### 3.2 微交互增强

| 场景 | 当前 | 强化 |
|------|------|------|
| 消息发送 | 直接出现 | 气泡从输入框位置弹入（transform + opacity 200ms） |
| 执行结果卡片 | 直接渲染 | 从 reply 气泡底部展开（max-height transition） |
| 删除操作 | 直接消失 | 左滑后淡出 + 高度收缩（200ms） |
| 下拉刷新 | 无 | 聊天页下拉刷新最近对话 |
| 空状态 | 文字 | 插画 + 文字 + 引导按钮 |
| 数据加载 | 无反馈 | 骨架屏（Skeleton）|

#### 3.3 性能优化

| 项目 | 方案 |
|------|------|
| 虚拟列表 | 长对话/长账单列表使用 `uni-list` 或自研虚拟滚动 |
| 图片懒加载 | 日记附件、执行结果图标懒加载 |
| 存储分层 | 热数据（当月）放内存缓存，冷数据按需读取 |
| 防抖统一 | 所有 input 事件统一走 `utils/debounce.js` |
| 首屏优化 | Splash → 关键模块 → 延迟非关键（已有，继续优化延迟链） |

#### 3.4 小程序适配

**当前**：`mp-weixin` 配置不完整。

**方案**：
- 补全 `manifest.json` 中微信小程序 appid
- 条件编译处理平台差异（通知、录音、存储限制）
- 微信小程序体积限制（2MB 主包），可能需要分包加载

#### 3.5 数据安全

| 项目 | 方案 |
|------|------|
| 本地数据加密 | 敏感数据（如 AI API Key）使用 `uni.setStorageSync` + 加密 |
| 导出数据脱敏 | 导出时可选是否包含个人信息 |
| 安全键盘 | PIN 码输入使用 `password` 类型键盘 |
| 防截屏 | 隐私页面（PIN 输入、个人信息）禁止截屏 |

---

## 三、优先级矩阵

```
紧急且重要（立即做）          重要但不紧急（计划做）
┌─────────────────────┬─────────────────────┐
│ P0-1 TabBar 乱码修复  │ P1-1 语音输入修复     │
│ P0-2 AI 响应可靠性    │ P1-2 数据可视化       │
│ P0-3 chat/index 拆分  │ P1-3 全局搜索         │
│ P0-4 store/data 拆分  │ P1-4 对话管理 UX      │
├─────────────────────┼─────────────────────┤
│ 紧急但不重要          │ 不紧急不重要          │
│ P2-1 清理瘦身         │ P2-4 小程序适配       │
│ P2-2 微交互增强       │ P2-5 数据安全         │
│ P2-3 性能优化         │                      │
└─────────────────────┴─────────────────────┘
```

---

## 四、预期成果

### 4.1 用户体验指标

| 指标 | 当前 | 目标 |
|------|------|------|
| AI 响应准确率 | ~85% | >95% |
| 首屏加载时间 | ~1.5s | <1s |
| 页面切换流畅度 | 偶发卡顿 | 60fps |
| 包体积 | ~8MB（估） | <6MB（清理后） |

### 4.2 代码质量指标

| 指标 | 当前 | 目标 |
|------|------|------|
| 最大单文件行数 | 1900 行（chat/index.vue） | <500 行 |
| 硬编码颜色数 | 0 | 0（保持） |
| 重复代码块 | 多处 | 消除 |
| 组件复用率 | ~40% | >70% |

### 4.3 功能完整度

| 模块 | 当前 | 目标 |
|------|------|------|
| AI 对话 | 85% | 95% |
| 数据可视化 | 10% | 80% |
| 全局搜索 | 0% | 90% |
| 通知推送 | 20% | 85% |
| 语音输入 | 60% | 90% |
| 对话管理 | 50% | 85% |

---

## 五、技术依赖

| 依赖 | 用途 | 体积 | 引入方式 |
|------|------|------|----------|
| uCharts | 图表库 | ~200KB | uni_modules |
| 无新增依赖 | — | — | 优先用原生能力 |

**移除依赖**：
| 依赖 | 原因 | 节省 |
|------|------|------|
| crypto-js.min.js | 改用 Web Crypto API | 60KB |

---

## 六、风险与应对

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| chat/index.vue 拆分引入 Bug | 高 | 功能异常 | 逐组件迁移，每步真机验证 |
| uCharts 跨端兼容问题 | 中 | 图表不显示 | fallback 到纯 CSS 图表 |
| Web Crypto API 在 App 端不支持 | 中 | 语音签名失败 | 条件编译，App 端用 plus.crypto |
| 小程序分包配置复杂 | 低 | 包体积超限 | 优先保证 H5+App，小程序延后 |

---

> **执行建议**：从 P0-1（TabBar 乱码）开始，5 分钟可见效果。然后 P0-2（AI 可靠性），这是用户感知最明显的提升。拆分工作（P0-3/4）建议在功能稳定后进行，避免重构引入新 Bug。
