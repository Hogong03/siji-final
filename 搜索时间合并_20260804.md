# 搜索与时间合并 — 2026-08-04

## 目标
将搜索功能和时间范围搜索合并为一个统一的搜索体系，去掉割裂的"全局/本月"开关。

## 改动前问题
- 搜索有独立的 `globalSearch` 开关，打开后走 `doGlobalSearch()` 遍历所有分片
- 搜索时隐藏分类、标签、时间范围 section — 搜索和筛选不能同时生效
- `globalSearchResults` 和 `filteredDiaries` 是两套数据源，渲染逻辑分叉

## 改动内容

### composable（`useDiaryList.js`）
- **删除** `globalSearch` ref、`doGlobalSearch()` 函数、`globalSearchResults` ref、`isGlobalSearching` ref
- **`switchMonth()` 不再清空** searchKeyword/filterCategory/filterTag — 时间切换只重载数据，保留筛选条件
- **`filteredDiaries`** 是唯一数据源：在 `loadDiaries()` 加载的时间范围数据上，同时做搜索+分类+标签过滤
- **`calendarDays`** 改为基于 `filteredDiaries` 而非 `diaries` — 日历也响应搜索筛选
- **`hasActiveFilter`** 去掉 globalSearch 判断
- **`resetFilters()`** 去掉 globalSearch 清除

### 模板（`list.vue`）
- 去掉全局/本月开关（`.fp-global-toggle`）
- 去掉 `globalSearchResults` 渲染分支和 `isGlobalSearching` 状态
- 时间范围 section 始终显示（不因搜索关键词而隐藏）
- 分类、标签 section 始终显示（不因搜索关键词而隐藏）
- 内容区统一用 `filteredDiaries` 一个数据源
- 搜索输入去掉 `@input`/`@confirm` 事件（不再需要手动触发 doGlobalSearch，filteredDiaries 是 computed 自动响应）

### 样式（`list.scss`）
- 移除 `.fp-global-toggle` 及其深色模式规则

## 效果
- 搜索 + 时间 + 分类 + 标签 **四个维度同时生效**，互不干扰
- 选"全部时间" + 输入关键词 = 跨月搜索
- 选具体月份 + 输入关键词 = 月内搜索
- 切换时间范围时搜索关键词保留
- 日历视图也响应搜索筛选

## 测试
81/81 通过，0 回归。
