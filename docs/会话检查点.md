# 思迹 · 会话检查点

> 用途：对话上下文被压缩、压缩失败、或换了新对话时的恢复入口。
> **新对话第一件事：读这个文件。** 上一个任务线程 `01a01c09` 在压缩时崩了（远端 compact 硬失败），上下文没保住，靠这个文件续。
> 最后更新：2026-09-18

---

## 一、当前状态（实测，非推断）

| 项 | 值 |
|---|---|
| 仓库版本 | `manifest.json` **4.3.0 / 430** |
| HEAD | `cec9d57` fix: 4.2.1 记录筛选面板改成底部弹出 |
| 远端 | `origin/main`，与本地**已同步**（无 ahead/behind） |
| 全量测试 | **77 文件 / 1119 用例全绿**（`npx vitest run --maxWorkers=2`，exit 0） |
| 工作区 | 干净（4.3.0 已收尾发版并推送） |

---

## 二、上一轮对话（`01a01c09`）的完整脉络

一个线程从 3.6.0 一路做到 4.2.1，共 17 个版本。按时间倒序：

| 版本 | 做了什么 |
|---|---|
| 4.2.1 | 记录筛选面板改底部弹出（原来从顶部滑出被导航栏盖住） |
| 4.2.0 | **记录模块收敛**：类型 5→3、分类并入标签、筛选 4→2、加「翻一翻」回顾卡、AI 自动打标签。方案对比图在 `docs/记录方案对比.html` |
| 4.1.2 | 目录尺重画（脊柱 + 20/34rpx 刻度）+ 藏掉系统滚动条 |
| 4.1.1 | 目录尺刻度全堆顶部 —— `splitSections` 漏了 `percent` |
| 4.1.0 | 六级复习资料 6 篇入库 + 记录阅读页版式重做 |
| 4.0.1 | 版本历史页不再把「日志最新」写成「当前版本」 |
| 4.0.0 | 版本号统一到 4.x（修版本号一直显示 1.0.0） |
| 3.10.2 | 计划有时间却不显示 —— 回填补 `start_time`/`end_time` 回落 + 摘要带时刻 |
| 3.10.1 | 计划没时间的真因 —— 补节假日表 + 注入 prompt + 执行器日期兜底 |
| 3.10.0 | 开场对话整合 + 按钮全部可点（修 `$root.$emit` 与 `uni.$on` 不通的死按钮）+ 计划时间可改 + 到点默认提醒 + 文件类型放宽 |
| 3.9.0 | 记录长文阅读页 + 左侧目录尺；六级技巧改 4 章长文 |
| 3.8.0 | 内置 12 条六级技巧到记录（标签：技巧） |
| 3.7.9 / 3.7.8 / 3.7.7 | Android 选文件三次真因：Uri 不能字符串化 → 类没导入 → 流对象的方法也要导入（`invokeSafe` 铺满拷贝层） |
| 3.7.5 | Android 选文件走 `plus.android` 调系统选择器（零插件零权限） |
| 3.7.3 | **确认闸门被绕过**（大额记账直接落库）+ 拍平模型嵌套的 `multi` |
| 3.7.2 | 自检语料改绑真实数据（`{plan}` / `{billAmount}` 占位符）+ 拦住幻觉动作类型（实测出现过 `batch`） |
| 3.6.1 | 「回去接着聊」跳错目标 —— 落盘白名单丢了 `_isWelcome` 等标记 |
| 3.6.0 | 读网址（直连优先 + 失败兜底 Tavily）与读文件（文本直读 + 文档解析） |

---

## 三、4.3.0 长文能力（已发版）

上一轮最后一条指令是「全部执行」，代码写完后线程就崩了（未提交）；本线程于 2026-09-18 收尾发版。

**已完成（代码在，测试绿）**

- 输出上限：`utils/ai/providers.js` 新增 `PROVIDER_MAX_TOKENS`（deepseek/qwen/moonshot 8192、zhipu 4096）+ `getMaxTokens()`；四条请求路径都带上 `max_tokens` —— `chat-sse.js` / `chat-chunked.js` / `agent-transport.js`（工具轮 + 最终流式两处）/ `buildProviderRequest`
- 提示词长文例外：`prompt-actions.js` 的 `BEHAVIOR_RULES` 新增长文规则（不受「闲聊 1-3 句」约束、用 `##` 分小节、800-3000 字、写完必须 `create_diary`）；`CORE_ACTIONS` 的 `create_diary` 点明「长文也走这个工具」
- 核心铁律 1 标了适用范围：`prompt-builder.js` 的「闲聊 1-3 句」加「（日常闲聊适用）」
- 长文入口：`MessageBubble.vue` 正文 ≥800 字给「按章节阅读」（`isLongText` + `read-long` 事件）；`pages/chat/index.vue` 的 `handleReadLong` 接住 —— 已存过记录直接进阅读页，没存过就落一条再读
- 自检语料：`utils/ai/eval/cases.js` 新增 `long-form-article`（要求 1000 字文章 + 落库正文 ≥600 字，顺带实测各家输出上限）
- 测试：`tests/long-form.test.js`（新增，未跟踪），全量 77 文件 / 1119 用例全绿

**收尾时补上的**

1. `.bubble-action-strong` 补了样式（黑描边胶囊 + 反相按压，深色模式跟随）
2. `manifest.json` → 4.3.0 / 430
3. 版本日志新增 `utils/storage/version-log/4.3.js` 并接线 `version-data.js`
4. `AGENTS.md` / `CODEX_HANDOFF.md` / `docs/AI效果自检基线.md` / 本文件同步（语料 23 → 24 条）
5. 已 commit + push

**仍未做**

- 长文规则**没在真机实测**（本机无 API Key、无 `@dcloudio` 编译依赖）
- 改过提示词，按规矩要**重跑 AI 效果自检并登记一行**；本机无 Key，没跑

---

## 四、悬着的真机验证项（累计）

都在等 HBuilder X 真机跑一遍，逐条登记进 `docs/真机验证清单.md`：

1. **Android 选文件**：3.7.9 把流对象方法收进 `invokeSafe` 之后是否通了（历史上停在 `stream-fail:input.read is not a function`）
2. **记录阅读页目录尺**：左侧尺在不在、点刻度能不能跳、拖动有没有章节名浮层（3.9.0 → 4.1.2 一路在改）
3. **记录模块 4.2.0**：顶部「翻一翻」回顾卡点得进、标签快捷条能筛、搜索框「上周的工作记录」同时换时间与关键词、老记录类型变「记录」且原分类进了标签
4. **开场消息按钮 + 计划时间**：欢迎语三个按钮可点、计划详情改开始/截止时间能存住、到点有没有系统通知（首次要允许通知权限）
5. **长文能力（4.3.0）**：让它写一篇长文，看落库正文够不够长、点「按章节阅读」能不能进阅读页

---

## 五、为什么上一轮的压缩会失败（根因）

压缩不是本地做的，是**远端 compact 服务**跑的一次调用。那条线程的失败记录给了两条硬错误：

```
Error running remote compact task: {"error":{"message":"The supported API model names are
deepseek-flash, deepseek-v4-pro, but you passed glm-5.3-flash.", ...}}

Error running remote compact task: Fatal error: remote compaction v2 expected exactly one
compaction output item, got 0 from 2 output items
```

结论：**远端压缩只接受 deepseek 系模型**，那条线程跑的是 `glm-5.3-flash` → 直接 `invalid_request_error`；换成 deepseek 之后又撞上第二个错（压缩输出项数量不符，0 个）。两次都是 harness 侧的问题，不是项目代码的问题，**在对话内部改不动**。

能做的：① 出问题就用 deepseek 系的线程；② 靠这个检查点文件兜底 —— 上下文没了但结论还在。

---

## 六、恢复姿势

新对话（或压缩后）按这个顺序接上：

1. 读本文件 → 读 `AGENTS.md`（速查表与红线）→ 需要细节再看 `CODEX_HANDOFF.md`
2. `git status --short` 先看工作区干不干净 —— 有未提交改动先问清楚要不要续
3. 接活时按项目的发布规矩走：全量测试跑绿 → `manifest.json` 提版本 → 版本日志加一条 → `git add -A` → commit → push（推送失败当场报告，不改 `sslVerify`）
4. **每个里程碑（发版成功后）回来更新本文件的第一节和第三节**
