# 记录列表方案A — 统一筛选面板 — 2026-08-04

## 目标
将记录列表页的搜索、分类、标签、统计、视图切换从多个独立横栏整合到一个顶部下滑筛选面板中，使主布局只剩「顶栏 → 内容 → FAB」三段。

## 改动

### composable 层
- **`useDiaryList.js`**：新增 `hasActiveFilter` 计算属性（判断是否有激活筛选条件）和 `resetFilters()` 方法（一键清空所有筛选）。默认月份从 `months[0]`（"全部时间"）改为 `months[1]`（当月）。

### 模板层
- **`list.vue`** 完整重写：
  - 移除独立的搜索栏、筛选栏、统计行、视图切换区域
  - 新增 `.filter-overlay`（遮罩）+ `.filter-panel`（下滑面板），包含：
    - 搜索输入 + 全局/本月开关
    - 时间范围（月份横滑，与顶栏同步）
    - 分类 chip 横滑
    - 标签 chip 横滑
    - 视图切换（列表/时间线/日历）
    - 统计（可展开详情）
    - 重置筛选按钮（仅有筛选条件时显示）
  - 顶栏🔍图标增加红点指示器（有筛选条件时显示）
  - 面板从顶部下滑展开（`transform: translateY(-100%)` → `translateY(0)`，0.25s ease-out）

### 样式层
- **`list.scss`** 完整重写：
  - 移除 `.search-bar` `.filter-bar` `.stats-mini` `.stats-detail` `.view-toggle` 等旧样式
  - 新增 `.filter-panel` `.filter-overlay` `.fp-*` 系列面板样式
  - 深色模式完整适配

## 主页面结构（简化后）
```
顶栏(月份横滑 + 🔍 + 🗑) → 内容(列表/时间线/日历) → FAB
```
筛选时：遮罩 + 顶部下滑面板（搜索+时间+分类+标签+视图+统计+重置）

## 测试
81/81 通过，0 回归。
